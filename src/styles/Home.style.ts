import { createStyles } from '@material-ui/core'

const homeStyle = () =>
  createStyles({
    contentBase: {
      margin: '10px'
    },
    input: {
      display: 'none'
    }
  })

export default homeStyle
